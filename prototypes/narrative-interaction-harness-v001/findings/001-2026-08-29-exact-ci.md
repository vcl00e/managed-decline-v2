# Narrative Interaction Harness v001 — exact-branch CI verification

**Date:** 2026-08-29

**Commit verified:** `fe4e54bfe790950c11b57af2adbd724b83f0e7ab`

The exact committed branch passed:

- syntax checks;
- 8 unit / contract tests;
- 4 rendered-client browser tests using real keyboard input;
- historical v007d stale-affordance regression audit;
- HTTP server smoke test.

The rendered-client tests cover:

1. intended complete route through map → focused interaction → map consequence;
2. rapid/repeated interaction input cannot consume or chain stale affordances;
3. cancelling a focused interaction returns to a resumable live situation;
4. an uninformed policy following only visible prompts can complete a run.

The historical regression fixture is expected to fail audit with repeated-action and repeated-output errors. Its rejection is a passing release-gate condition.

This evidence establishes runtime/release-gate reliability for the deliberately simple harness fixture. It does not establish narrative quality.